#include <iostream>

using namespace std;

int main()
{
    int T, X, Y, Z, exp;
    char name[50], op[5];
    cin >> T;

    for(int i = 1; i <= T; i++)
    {
            cin >> X >> Y >> Z;
            cin >> name >> exp >> op;


    }
    return 0;
}
